export const metadata = {
  title: "Guestbook",
  description: "Leave a message, feedback, or say hello on Syahreza Satria's personal portfolio guestbook.",
};

export default function GuestbookLayout({ children }) {
  return <>{children}</>;
}
